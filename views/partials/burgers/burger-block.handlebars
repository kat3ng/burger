<li>
    {{name}}

    <button class="change-status" data-id="{{id}}" data-newsleep="{{sleep}}">
        {{#if sleep}}Available{{else}}Devoured!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>